import styled from 'styled-components'

export const LayoutStyle = styled('div').attrs({
    id: 'home',
})`
    width: 100%;
    height: 100%;
    background-color: #fff;
`

export const ContentWrapperStyle = styled.div`
    margin: 0 auto;
    max-width: 100%;
    height: 100%;
    position: relative;
`
